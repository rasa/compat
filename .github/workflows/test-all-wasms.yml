name: test-all-wasms

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-js:
    uses: ./.github/workflows/test-js.yml
    secrets: inherit
  test-wasip:
    uses: ./.github/workflows/test-wasip1.yml
    # needs: test-js
    # if: success()
    secrets: inherit
