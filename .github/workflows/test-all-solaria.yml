---
# yaml-language-server: $schema https://json.schemastore.org/github-workflow.json

name: test-all-solaria

on:
  workflow_call:
  workflow_dispatch:

# mega-linter needs:
permissions:
  contents: read

jobs:
  test-illumos:
    uses: ./.github/workflows/test-illumos.yml
    secrets: inherit
  test-solaris:
    uses: ./.github/workflows/test-solaris.yml
    # needs: test-illumos
    # if: success()
    secrets: inherit
